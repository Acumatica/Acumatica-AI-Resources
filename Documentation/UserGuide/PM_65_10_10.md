# Project Cash Flow {#_8c309e69-2e5b-48de-95f1-9117397c6881 .reference}

Form ID: \(PM651010\)

**Attention:** This generic inquiry form is available if the *Extended Financial Data Collection* feature is enabled on the [Enable/Disable Features](CS_10_00_00.md) \(CS100000\) form.

This generic inquiry form provides visibility into your project's cash flow as of the date you specify. You can identify potential shortfalls or surplus funds and ensure that each project has enough cash to cover its expenses.

Initially, the form shows projects with all statuses, but you can filter the data in the table to show projects with any status.

## Form Toolbar { .section}

The form toolbar has only standard buttons.

## Selection Area { .section}

In this area, you select the date through which the system shows projects in the table.

|Element|Description|
|-------|-----------|
|**Date**|The date through which the data is displayed—that is, you’ll see project data up to and including this date. By default, the system inserts the current business date.|

## Table { .section}

In the table, you can view each project’s date-specific cash flow, revenue budget backlog, and cost budget backlog. You can also see the total amounts across all listed projects. Each row represents a project.

Below you can find information about the columns that contain the project’s calculated values, along with the formulas that the system uses for these calculations.

**Attention:**

-   The leftmost columns of the table aren't described in this topic. These columns display the project's basic information—such as project manager, description, status, company, branch, currency, and start and end dates. If you need information about these UI elements, see their descriptions on the [Projects](PM_30_10_00.md) \(PM301000\) form.
-   The values are calculated **in the project currency**. The form shows values in the base currency and the project currency if the *Multicurrency Accounting* and *Multicurrency Projects* features are enabled on the [Enable/Disable Features](CS_10_00_00.md) \(CS100000\) form.

The table toolbar has only standard buttons.

|Column|Description|
|------|-----------|
|**Revised Contract**|The revised budgeted amount of the project's revenue budget, which the system calculates as the sum of the **Revised Budgeted Amount** values specified on the **Revenue Budget** tab of the [Projects](PM_30_10_00.md) \(PM301000\) form.**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Revised Contract \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Budgeted Cost**|The revised budgeted amount of the project's cost budget, which the system calculates as the sum of the **Revised Budgeted Amount** values specified on the **Cost Budget** tab of the [Projects](PM_30_10_00.md) \(PM301000\) form.**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Budgeted Cost \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Gross Profit**|The gross profit, which the system calculates by using this formula: `Revised Contract - Budgeted Cost`.**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Gross Profit \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Margin \(%\)**|The margin percentage, which the system calculates by using this formula: `Gross Profit / Revised Contract`.|
|**Revenue to Date**|The total amount of released project transactions with the *AR* source and a date earlier than or the same as the date specified on the current form.**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Revenue to Date \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Expenses to Date**|The total amount of released cost transactions with the date earlier than or the same as the date specified on the current form.**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Expenses to Date \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Completed \(%\)**|The percent of project completion, which the system calculates by using this formula: `Expenses to Date / Budgeted Cost`.If the budgeted cost is *0*, the **Completed \(%\)** will also be *0*.

|
|**Gross Profit to Date**|The gross profit to date, which the system calculates by using this formula: `Revenue to Date - Expenses to Date`.**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Gross Profit to Date \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Margin to Date \(%\)**|The margin percentage as of a specified date, which the system calculates by using this formula: `Gross Profit to Date / Revenue to Date`.If the **Revenue to Date** amount is *0*, the margin percentage will also be *0*.

|
|**Open AR Amount**|The open AR amount, which the system calculates by using this formula. ``` {#codeblock_ly2_3nn_3hc}
AR Amount (Project Curr.) - AR Paid Amount (Project Curr.) - AR Cash Discount (Project Curr.) - Write-Off Amount (Project Curr.) - Realized Gain/Loss (Project Curr.)
```

For a complete description of how the system calculates the values in this formula, refer to the [Project AR Details](PM_65_10_02.md) \(PM651002\) form reference topic.

**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Open AR Amount \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**AR Retainage**|The AR retainage amount, which the system calculates by using this formula.```
AR Retained Amount (Project Curr.) + AR Retained Taxes (Project Curr.)
```

For a complete description of how the system calculates the values in this formula, refer to the [Project AR Details](PM_65_10_02.md) \(PM651002\) form reference topic.

**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **AR Retainage \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**AR Difference**|The AR difference amount, which the system calculates by using this formula: `AR Cash Discount (Project Curr.) + Write-Of Amount (Project Curr.) + Realized Gain/Loss (Project Curr.)`.For a complete description of how the system calculates the values in this formula, refer to the [Project AR Details](PM_65_10_02.md) \(PM651002\) form reference topic.

**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **AR Difference \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Taxes on Open AR**|The tax amount from unpaid customer invoices for this project, which the system calculates by using this formula.```
Inclusive Taxes on AR (Project Curr.) + Exclusive Taxes on AR (Project Curr.) - Paid Inclusive Tax (Project Curr.) - Exclusive Taxes on Paid AR (Project Curr.)
```

For a complete description of how the system calculates the values in this formula, refer to the [Project AR Details](PM_65_10_02.md) \(PM651002\) form reference topic.

**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Taxes on Open AR \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Open AP Amount**|The open AP amount, which the system calculates by using this formula.```
AP Amount (Project Curr.) - AP Paid Amount (Project Curr.) - AP Cash Discount (Project Curr.) - Withheld Tax Amount (Project Curr.) - Realized Gain/Loss (Project Curr.)
```

For a complete description of how the system calculates the values in this formula, refer to the [Project AP Details](PM_65_10_01.md) \(PM651001\) form reference topic.

**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Open AP Amount \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**AP Retainage**|The AP retainage amount, which the system calculates by using this formula: `AP Retained Amount (Project Curr.) + AP Retained Taxes`.The values in the formula are from the [Project AP Details](PM_65_10_01.md) \(PM651001\) form.

**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **AP Retainage \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**AP Difference**|The AP difference amount, which the system calculates by using this formula.```
AP Cash Discount (Project Curr.) + Withheld Tax Amount(Project Curr.) + Realized Gain/Loss (Project Curr.)
```

For a complete description of how the system calculates the values in this formula, refer to the [Project AP Details](PM_65_10_01.md) \(PM651001\) form reference topic.

**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **AP Difference \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Taxes on Open AP**|The tax amount from unpaid vendor bills for this project, which the system calculates by using this formula.```
Billed Inclusive Tax (Project Curr.) + Billed Tax (Project Curr.) - Paid Inclusive Tax (Project Curr.) - Exclusive Taxes on Paid AP (Project Curr.)
```

For a complete description of how the system calculates the values in this formula, refer to the [Project AP Details](PM_65_10_01.md) \(PM651001\) form reference topic.

**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Taxes on Open AP \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Cash Inflow to Date**|The project's cash as of the specified date, which the system calculates by using this formula: `Revenue to Date + Taxes on Open AR Amount - Open AR Amount - AR Retainage - AR Difference`.**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Cash Inflow to Date \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

The system calculates the project's cash inflow to date by using an indirect method.

|
|**Cash Outflow to Date**|The project's cash outflow as of the specified date, which the system calculates by using this formula: `Expenses to Date - Open AP Amount + Taxes on Open AP Amount - AP Retainage - AP Difference`.**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Cash Outflow to Date \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

The system calculates the project's cash outflow to date by using an indirect method.

|
|**Cash Flow to Date**|The project's cash flow as of the specified date, which the system calculates by using this formula: `Cash Inflow to Date - Cash Outflow to Date`.**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Cash Flow to Date \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Backlog Cash Inflow**|The forecasted cash inflow as of the specified date, which the system calculates by using this formula: `Revenue Budget - Cash Inflow to Date`.**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Backlog Cash Inflow \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Backlog Cash Outflow**|The forecasted cash outflow as of the specified date, which the system calculates by using this formula: `Budgeted Cost - Cash Outflow to Date`.**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Backlog Cash Outflow \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Backlog Cash Flow**|The forecasted cash flow as of the specified date, which the system calculates by using this formula: `Backlog Cash Inflow - Backlog Cash Outflow`.**Tip:** This value is calculated in the project currency. If multicurrency projects are set up in your system, you'll also see the **Backlog Cash Flow \(Base Curr.\)** column showing the value in the base currency. The system calculates it similarly.

|
|**Base Currency**|The base currency used for accounting in the current branch.|
|**Currency Rate**|The exchange rate from the project currency to the base currency. This rate is used to display amounts in the project and base currencies if the *Multicurrency Accounting* and *Multicurrency Projects* features are enabled on the [Enable/Disable Features](CS_10_00_00.md) \(CS100000\) form.

|
|**Rate Variance for Cash Flow to Date \(Base Curr.\)**|The rate variance for cash flow to date, which the system calculates by using this formula: `Cash Flow to Date / Currency Rate - Cash Flow to Date (Base Curr.)`. The rate variance is the accumulated difference between:

 -   The cash flow converted from the project currency to the project’s base currency at the current project rate
-   The cash flow converted at the historical rate of the project’s base currency

|

**Parent topic:**[Construction Forms](../UserGuide/Construction__CN_Form_Reference.md)

