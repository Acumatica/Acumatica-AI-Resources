paths-ignore:
  # Expired, self-signed training certificate documented as non-production.
  # Its exact path and SHA-256 are enforced by validate-contribution.ps1.
  - "Documentation/UserGuide/Files/AcumaticaTrainingEncryption.pfx"
